name: Build release

on: [push, workflow_dispatch]

jobs:
  release:
    if: ${{ endsWith(github.event.head_commit.message, '(release)') || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: 'main'

      - name: Fetch xbps-src
        run: |
          cd package
          ./xbps-src-setup.sh

      - name: Fetch xbps binaries
        run: |
          mkdir -m0755 ~/.xbps
          curl -#Lo- http://repo-default.voidlinux.org/static/xbps-static-latest.x86_64-musl.tar.xz | unxz | tar -xC ~/.xbps

      - name: Build kickit
        run: |
          export PATH=$HOME/.xbps/usr/bin:$PATH
          cd package
          ./xbps-src pkg kickit

      - name: Fetch artifacts
        uses: actions/download-artifact@v6

      - name: Upload package
        uses: actions/upload-artifact@v5
        with:
          name: release
          path: package/hostdir/binpkgs/main/*
