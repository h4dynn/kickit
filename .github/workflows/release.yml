name: Build release

on: [push, workflow_dispatch]

jobs:
  release:
    if: (${{ github.event_name == 'push' }} && contains(github.event.head_commit.message, '(build)')) || true
    uses: ./.github/workflows/build.yml
    release: true
    name: Build release
